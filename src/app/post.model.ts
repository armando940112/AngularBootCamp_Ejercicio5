export class Post {
    // userId: number;
    // Id: number;
    // Title: string;
    // Body: string;
    constructor(public id: number, public title: string, public body: string) {
        // this.Id = id;
        // this.Title = title;
        // this.Body = body;
    }
}